sudo: required

services:
  - docker

language: go

go:
  - 1.7.4

before_install:
  - ci/install-docker-compose
  - ci/install-glide
  - ci/install-golint
  - make test
  - make bin
  - ci/setup-docker-network
  - ci/setup-dbs
  - make integration

cache:
  directories:
    - $HOME/.glide/cache
